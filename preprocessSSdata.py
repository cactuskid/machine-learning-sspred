from Bio import SeqIO
import functions
import

extract_protsequences = True
cluster_sequences = True
recompile_joint_file = True


extract_protsequences == True:
    #grab only the prot sequences


cluster_sequences == True:
    #cdhit
    #grab the IDS

recompile_joint_file == True:
    #parse cdhit fasta
